<h1>Informations détaillée sur l'entité <%= @body.short_name %></h1>
<p><strong>Nom : </strong><%= @body.short_name %></p>
<p><strong>Prénom : </strong><%= @body.long_name %></p>
<p><strong>Commentaire : </strong><%= @body.comment %></p>
<table class='table table-striped table-sm'>
  <thead>
    <tr>
      <th scope="col">Acronyme</th>
      <th scope="col">Nom</th>
      <th scope="col">Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @body.descendants.each do |body| %>
      <tr>
        <th scope="row"><%= body.short_name %></th>
        <td><%= body.long_name %></td>
        <td>
          <ul class="list-inline mb-0">
            <li class="list-inline-item">
              <%= link_to body_path(body) do %><i class="fas fa-eye m-0"></i><% end %>
            </li>
            <li class="list-inline-item">
              <%= link_to edit_body_path(body) do %>
                <i class="fas fa-edit list-inline-item"></i>
              <% end %>
            </li>
            <li class="list-inline-item">
              <%= link_to body_path(body), method: :delete, data: {confirm: "Êtes vous sûr ?"} do %>
                <i class="fas fa-trash-alt list-inline-item"></i>
              <% end %>
            </li>
          </ul>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to "Retour à la liste d'entité", bodies_path, class: 'btn btn-flat btn-sm' %>

<%= simple_form_for(@expert) do |f| %>
  <%= f.association :title, label_method: :long_title, collection: Title.order(:long_title), include_blank: false %>
  <%= f.input :last_name %>
  <%= f.input :first_name %>
  <%= f.submit class: 'btn btn-flat'%>
<% end %>
